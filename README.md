# Directory-Analyser
A scallable and flexible directory anallyser. Usable for getting information about the size of a directory (and it's subfolders) separated by categories.
It generates a .csv file containing the top 5 biggest files in a directory.
Made with Python
